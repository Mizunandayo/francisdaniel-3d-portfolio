// Implemented: Day 14


import type { Vector3 } from 'three'

export function useProximity(
  _objectPosition: Vector3,
  _threshold: number
): boolean {
  // Day 14: reads avatarPosition from sceneStore, checks distance in useFrame
  return false
}