// Implementation: Day 13


/**
 * Slides in from the right when the TV is activated.
 * Renders SpotifyConnect if no token, NowPlaying if connected.
 * Closes on Escape key or X button click.
 * Consistent with all other panels in the scene.
 */

export function TVPanel() {
  // Day 13: AnimatePresence, motion.div x: '100%' -> 0, SpotifyConnect/NowPlaying
  return null
}